input:
  generate:
    mapping: |
      id = ksuid()
      event = "order.created"
      ts = timestamp_unix()
      data.customer_id = ksuid()
      items = range(0, random_int(min: 1, max: 3)).map_each(item -> {
        "product_id": "replace_me",
        "unit_price": fake("amount_with_currency"),
        "quantity": random_int(min: 1, max: 10),
      })
